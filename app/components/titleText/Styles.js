import { StyleSheet } from 'react-native';
import { COLOR_GRAY } from '../../styles/common';
export default StyleSheet.create({
  textTitle: {
    fontSize: 17,
    fontWeight: '300',
    color: COLOR_GRAY,
    marginTop: 5,
    marginBottom: 5,
    textAlign: 'left',
    textAlignVertical: 'center',
  }
});
