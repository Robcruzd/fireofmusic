import { StyleSheet } from 'react-native';
import { COLOR_RED } from '../../styles/common';
export default StyleSheet.create({
  textTitle: {
    fontSize: 15,
    color: COLOR_RED,
    marginTop: 5,
    marginBottom: 5,
    textAlign: 'center',
    textAlignVertical: 'center',
  }
});
